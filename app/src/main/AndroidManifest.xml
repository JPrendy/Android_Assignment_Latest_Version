<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ie.dit.james.android_assignment_latest_version" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--this allows access to New_Movie class with its screen orientation locked to portrait-->
        <activity
            android:name=".New_Movie"
            android:screenOrientation="portrait"
            android:label="" >
        </activity>
        <!--this allows access to EditContract class with its screen orientation locked to portrait-->
        <activity
            android:name=".Edit_Movie"
            android:screenOrientation="portrait"
            android:label="" >
        </activity>
        <!--this allows access to WebPage class with its screen orientation locked to portrait-->
        <activity
            android:name=".WebPage"
            android:screenOrientation="portrait"
            android:label="" >
        </activity>
    </application>


</manifest>
